/* =====================================================================
   Aris — Combate (targeting de torres). Dueño: JULIAN.
   PLACEHOLDER de Fase 0 (contrato 3.3): tick no-op. Julian recorre torres en
   `world`, elige objetivo con enemies.list(), respeta cadencia (STRUCT.tower),
   rota userData.cannon y llama projectiles.fire.
   ===================================================================== */

export function tick(_dt) { /* PLACEHOLDER */ }
