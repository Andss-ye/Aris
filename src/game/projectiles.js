/* =====================================================================
   Aris — Proyectiles. Dueño: JULIAN.
   PLACEHOLDER de Fase 0 (contrato 3.3): API presente, tick no-op. Julian
   implementa pool + lerp + impacto/AOE que llama enemies.damage.
   ===================================================================== */

// fire(from, targetId, dmg, aoe) — encola un proyectil hacia un enemigo.
export function fire(_from, _targetId, _dmg, _aoe) { /* PLACEHOLDER */ }

export function tick(_dt) { /* PLACEHOLDER */ }
